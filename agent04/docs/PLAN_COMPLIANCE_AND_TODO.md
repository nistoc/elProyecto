# Соответствие плану миграции Agent04 и потенциальные доработки

Проверка по плану `agent04_.net_migration_plan_75488b61.plan.md`.

---

## 1. Что реализовано полностью

- **Этапы 0–10:** остов, Domain, Audio, Cache, OpenAI client, Output, Merger, Pipeline, REST API (POST/GET jobs, список, Location, 202), gRPC (SubmitJob, GetJobStatus, StreamJobStatus), IJobStatusStore, мониторинг, теги в REST и gRPC, кеширование (CachingJobStatusStore), Ninject (Agent04Module), модель узлов (INodeModel, INodeQuery, GET /jobs/{id}/nodes), RENTGEN_IMPLEMENTATION.md и скрипт регистрации факта, README, CHANGELOG.
- **Песочница (workspace_root):** обязательный WorkspaceRoot в appsettings/окружении, проверка при старте; configPath и inputFilePath в API — только относительные пути; уникальные имена транскриптов по jobId. Поддержка CLI удалена; запуск только как веб-сервис (HTTP + gRPC).
- **REST:** ProblemDetails для 400/404, OpenAPI с тегами и описаниями для виртуальной модели (RENTGEN).
- **Виртуальная модель:** запрос по jobId, список по тегу (GET /jobs/query), дерево узлов по scopeId, кеш с TTL и инвалидацией.

---

## 2. Что могло быть не доделано или отличается от плана

### 2.1. REST API (этап 9 и раздел 3 плана миграции)


| Пункт плана                          | Статус        | Комментарий                                                                                                                                                                                                                    |
| ------------------------------------ | ------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Тело POST: «встроенный JSON конфига» | Не требуется. | Принимается только `configPath` и `inputFilePath`. Нет трубуется вариант передать конфиг inline в теле запроса, чтобы не услоднять реализацию. Конфиг должен быть заранее положен в папку проекта в корне рабочей директоории. |
| Multipart: файл + конфиг             | Не требуется  | Нет приёма аудиофайла через multipart/form-data вместе с конфигом. Все файлы кладутся заранее в папку проекта с аудиофайлов                                                                                                    |
| Workspace root / относительные пути  | Реализовано   | WorkspaceRoot в appsettings/окружении, пути в API относительные, абсолютный inputFilePath — 400.                                                                                                                               |
| Заголовок `X-Caller-Id`              | Реализовать   | В плане допущен на первом этапе; не проверяется и не описан в OpenAPI.                                                                                                                                                         |


**Потенциальные правки:** при необходимости — вариант SubmitJob с телом с inline-конфигом; multipart для загрузки файла — не требуется расширение; реализовать проверка/прокидывание `X-Caller-Id` и описание в OpenAPI.

### 2.2. Мониторинг и статус (раздел 5 плана миграции, этап 9c)


| Пункт плана                                   | Статус                                            | Комментарий                                                                                                                            |
| --------------------------------------------- | ------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| TotalChunks / ProcessedChunks                 | Нет в контракте. Добавить                         | В статусе есть ProgressPercent и CurrentPhase (текст вида "Transcribing chunk 3/10"), отдельных полей TotalChunks/ProcessedChunks нет. |
| Webhook / callback_url при завершении         | Реализовать обработку как опционального параметра | В плане было опционально; добавить                                                                                                     |
| Push при завершении (gRPC-сервис уведомлений) | Реализовать обработку                             | В плане было опционально; добавить                                                                                                     |


**Потенциальные правки:** при необходимости — опциональные поля в JobStatus и в proto; опционально `callback_url` или аналог в SubmitJob и вызов при Completed/Failed.

### 2.3. gRPC (этап 9b, раздел 6 плана миграции)


| Пункт плана           | Статус      | Комментарий                                                                                                      |
| --------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------- |
| gRPC `QueryJobs(...)` | Реализовать | В плане: REST и gRPC для запросов по семантическому ключу. REST GET /jobs/query есть; в proto нет rpc QueryJobs. |
| Исходящий gRPC-клиент | Реализовать | В плане допущена заглушка на первом этапе; заглушки нет.                                                         |


**Потенциальные правки:** добавить в proto `rpc QueryJobs(QueryJobsRequest) returns (QueryJobsResponse)` и реализацию в TranscriptionGrpcService; при появлении смежных сервисов — опциональный исходящий gRPC-клиент (например, уведомления).

### 2.4. Виртуальная модель и JobQuery (этап 9d, раздел 6 плана миграции)


| Пункт плана                              | Статус                                               | Комментарий                                                                                                                                                                                                                               |
| ---------------------------------------- | ---------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Отдельный слайс Features/JobQuery        | Частично было оеализвоано. Дореализовать до конца    | IJobQueryService и кеш есть, но логика живёт в Features/Transcription, отдельной папки JobQuery нет. ВЫнести работу этой ыичи в отдельную папку                                                                                           |
| IJobQueryService.QueryBySemanticKey(...) | Отличие в имени. Выровнять имена                     | Фактически используется общий фильтр (tag, status, from, to, limit, offset) через IJobStatusStore.List(JobListFilter); отдельного метода QueryBySemanticKey с явным «семантическим ключом» в API нет (контракт покрыт query-параметрами). |
| Rate limit 429                           | Не реализовано. Добавить конфигурируемое ограничение | В плане было опционально; еще не добавлено.                                                                                                                                                                                               |


**Правки:** при желании строго следовать плану — вынести запросы/кеш в отдельный слайс Features/JobQuery; при необходимости — rate limit и 429.

### 2.5. Модель узлов (раздел 6.1 плана миграции)


| Пункт плана                          | Статус                                            | Комментарий                                                                                                                                                                      |
| ------------------------------------ | ------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| progressPercent, phase на узле       | Частично было оеализвоано. Дореализовать до конца | NodeInfo имеет ProgressPercent и Phase; pipeline не обновляет их на узлах (обновляются только статус и времена). Для отрисовки прогресса по узлам можно опираться на job status. |
| Метаданные корня (пути к артефактам) | Частично было оеализвоано. Дореализовать до конца | Пути отдаются в JobStatus (GET /jobs/{id}); в узлах метаданные не заполняются путями.                                                                                            |
| Атрибуты XRay                        | Не реализовано. Дореализовать                     | В плане опционально; обновление узлов делается явными вызовами в pipeline. Применить атрибуты XRay в основным бизнес-функциям сервиса                                            |


**Правки:** при необходимости — запись progressPercent/phase в узлы из pipeline или копирование из job status; при желании — пути в metadata корневого узла; XRay применить.

### 2.6. Прочее


| Пункт плана                                 | Статус                                 | Комментарий                                                                |
| ------------------------------------------- | -------------------------------------- | -------------------------------------------------------------------------- |
| Unit-тесты (этап 2: парсинг ffprobe и т.д.) | Не проверялось. Реализовать Unit тесты | В плане упомянуты unit-тесты; наличие тестов в репозитории не проверялось. |
| Аутентификация JWT (AUTH.md)                | Не в плане на первый этап              | По плану — позже; сейчас не реализовано.                                   |


---

## 3. OpenAPI и описание работы с RENTGEN

- В OpenAPI добавлены **теги** для группировки операций:
  - **Jobs** — создание и мониторинг заданий (POST/GET jobs, список).
  - **Virtual model (RENTGEN)** — виртуальная модель: запрос по тегу и дерево узлов.
- Для тега **Virtual model (RENTGEN)** в документе задано **описание**: назначение (запросы по семантическому ключу, иерархия узлов по job), типичное использование (GET /jobs/query, GET /jobs/{id}/nodes), частота опроса, ссылка на docs/RENTGEN_IMPLEMENTATION.md.
- У операций добавлены **summary** и **remarks**:
  - GET /jobs/{id}/nodes — иерархия шагов (job → chunking → transcribe → chunk-0..N → merge), параметр tree, поля узла (id, parentId, scopeId, kind, status, startedAt, completedAt, updatedAt), ссылка на RENTGEN_IMPLEMENTATION.md.
  - GET /jobs/query — запрос по семантическому ключу (тег), фильтры, полный статус по каждой записи, пригодность для опроса 0.01–10 Hz.

Внешний сервис по OpenAPI может: увидеть группу «Virtual model (RENTGEN)», прочитать описание тега и операций, понять, как получать список заданий по тегу и как получать дерево узлов по job id (flat или tree).

---

## 4. Рабочий каталог (песочница) — реализовано

- Реализовано: обязательный WorkspaceRoot в appsettings/окружении, проверка при старте; configPath и inputFilePath в REST/gRPC — только относительные пути; разрешение путей относительно workspace root в пайплайне; уникальные имена транскриптов (с jobId); создание подкаталогов агентом; удаление CLI, запуск только как веб-сервис.
- **Перспектива S3:** контракт «workspace root + пути относительно него» допускает в будущем подмену файлового слоя на S3 (workspace root = bucket/prefix) без изменения семантики для вызывающих.

---

## 5. Итог

- Основная функциональность плана реализована: остов, фичи транскрипции, REST, gRPC, мониторинг, теги, кеш, Ninject, модель узлов, RENTGEN-документ и скрипт, документация; **песочница (workspace_root)** и **удаление CLI** реализованы.
- Не реализованы или опциональны: встроенный конфиг и multipart в POST, X-Caller-Id, webhook/callback, gRPC QueryJobs, исходящий gRPC, отдельный слайс JobQuery, rate limit, TotalChunks/ProcessedChunks в контракте, XRay.
- Работа с API виртуальной модели (RENTGEN) описана в OpenAPI (теги, описания операций и тега), чтобы внешние сервисы могли интуитивно понять, как получать данные по этой логике.

